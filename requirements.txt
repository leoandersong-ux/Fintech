# Streamlit Community Cloud provides Streamlit and its direct dependencies
# (including pandas/pyarrow) in the managed runtime. Keep cloud deployment
# minimal to avoid rebuilding pyarrow on unsupported Python runtimes.
